'use strict'

import m from 'mithril'

const bank = {
  view: function () {
    return [
      m('p', 'Transfer in progress, will return to previously page shortly...')
    ]
  }
}

export default bank
